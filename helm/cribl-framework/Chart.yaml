apiVersion: v2
name: cribl-framework
description: >-
  Cribl onboarding framework application stack — portal (cribl-framework),
  cribl_service, ece_service, and etn_onboarding. Cribl Stream, Elasticsearch/
  Kibana (ELK), and PostgreSQL are NOT deployed by this chart; point it at
  external instances via the `external` values section.
type: application
version: 0.2.0
appVersion: "1.0.0"
kubeVersion: ">=1.25.0-0"
maintainers:
  - name: Cribl Framework team
